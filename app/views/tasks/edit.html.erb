<% provide(:title, 'Edit Todo Memo') %>
<div class="d-flex justify-content-end">
  <%= link_to '詳細へ', task_path(@task), class: 'nav-link' %>
</div>
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2>メモの編集</h2>
        <div class="form-wrapper">
          <%= render partial: 'form', locals: { task: @task } %>
        </div>
    </div>
  </div>
</div>
