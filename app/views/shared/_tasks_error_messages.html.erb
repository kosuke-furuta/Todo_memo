<% if @task.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      下記のフォームには次のものが含まれています。 <%= pluralize(@user.errors.count, "エラー") %>。
    </div>
    <ul>
    <% @task.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
