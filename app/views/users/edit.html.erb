<% provide(:title, 'Edit user') %>
<% provide(:button_text, '変更内容を保存する') %>
<div class="container">
  <div class="row">
    <div class="col-lg-6">
      <h1>プロフィールを編集</h1>
      <div class= "image">
        <%= form_with(model: @user, local: true) do |f| %>
          <% if @user.image? %>
            <%= image_tag @user.image.url %>
          <% else %>
            <%= image_tag('/images/icon/no-image.png') %>
          <% end %>
        <% end %>
      </div>
    </div>
    <div class="col-lg-6">
      <div class="form-wrapper">
        <%= render 'form' %>
      </div>
    </div>

  </div>
</div>
